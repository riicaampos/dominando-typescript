"use strict";
/**
 * Qual a diferença entre void e never??
 * Void = não ter um retorno
 * Never = um valor que nunca podera ser retornado
 * Então qual a diferença?
 * O void por exemplo se vc der um console.log em uma função que retorna void
 * irá retornar undefined, ja o never nunca será retornado
 */ 
